#include<stdio.h>
int main(void)
{
    char x, y, z;
    x = 0b11110100;
    y = 0b10110000;
    z = 2 * x + y / 2;
    char x = 65;
    printf("%dD = %oQ = %xH, and ASCII number of \'%c\' is %d\n", x,x,x,x,x);
    x= x^0x20;
    printf("%dD = %oQ = %xH, and ASCII number of \'%c\' is %d\n", x,x,x,x,x);
    return  0;
 /* 分别输出3个变量的十六进制（两位）和十进制形式 */
    return 0;

}

